import React from 'react';
import ReactDOM from 'react-dom/client';
import Arpy from './Arpy';

const root = ReactDOM.createRoot(document.getElementById('root')!);
root.render(
  <React.StrictMode>
    <Arpy />
  </React.StrictMode>
);

